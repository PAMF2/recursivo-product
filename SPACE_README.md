---
title: Recursivo Verify
emoji: 🔬
colorFrom: orange
colorTo: gray
sdk: docker
app_port: 7860
pinned: false
short_description: The verification layer for human-behavior simulation
---

# Recursivo - the verification layer for human-behavior simulation

A $2B category (Simile, Aaru, Humans&) sells AI that predicts how people behave, graded on
self-reported 85-99% accuracy. On public ground truth (Twin-2K-500, 2,058 real people), the best
model (GPT-4.1) predicts an individual's real answer **36%** of the time - below a dumb constant
guess (**58%**). Recursivo is the independent ruler.

- `/` landing · `/deck` investor deck · `/leaderboard` · `/items` · `POST /verify` · `POST /submit`

Thesis: *recursion only compounds where the outcome is verifiable.*
